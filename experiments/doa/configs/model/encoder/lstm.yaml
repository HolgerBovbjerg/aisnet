name: lstm
config:
    input_dim: ${model.encoder_embedding_dim}
    hidden_dim: ${model.encoder_embedding_dim}
    num_layers: 3
    bias: True
    dropout: 0.1
    projection_size: 0
    bidirectional: False