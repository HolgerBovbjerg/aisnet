enabled: True # Whether to use wandb
project: wandb_example_project # Name of your project
entity: null # If you have multiple associated entities (usernames/teams), pass entity name
name: ${job.exp_name} # Name of experiment
group: ${job.exp_name} # Specify a group to organize individual runs into a larger experiment.
job_type: null # Specify the type of run, e.g., "train".
tags: null # A list of strings, which will populate the list of tags on this run in the UI.
#resume: allow