test:
    data_sets:
        LibriSpeech:
            root: ${data_path}LibriSpeech/
            splits:
                - test-clean
    buffer_size: 10000
    load_from: raw
    load_from_tar: False
    segment_max_size: 96000
    max_token_count: 640000
    min_length: 400