name: lstm
config:
    input_dim: ${model.spectral_encoder_embedding_dim}
    hidden_dim: ${model.spectral_encoder_embedding_dim}
    num_layers: ${model.spectral_encoder_num_layers}
    bias: True
    dropout: 0.1
    projection_size: 0
    bidirectional: False