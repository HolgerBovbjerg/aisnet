name: lstm
config:
    input_dim: ${eval:'${model.spectral_encoder_embedding_dim}*2'}
    hidden_dim: ${eval:'${model.spectral_encoder_embedding_dim}*2'}
    num_layers: 3
    bias: True
    dropout: 0.1
    projection_size: 0
    bidirectional: False